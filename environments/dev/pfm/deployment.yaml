apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: pfm
  name: pfm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pfm
  strategy: {}
  template:
    metadata:
      labels:
        app: pfm
    spec:
      containers:
      - image: pahlevani/pfm:v0.0.1-alpha
        name: pfm
        resources: {}
